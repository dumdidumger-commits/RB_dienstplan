requests==2.32.3
pandas==2.2.3
openpyxl==3.1.5
PyYAML==6.0.2
# google-api-python-client/google-auth* 13.08.2026 entfernt (Roland-Wunsch): Google-Calendar-
# API-Sync komplett gestrichen, ICS-Kalenderabo ist der einzige Weg (siehe calendar_sync.py-
# Docstring und project_vivendi_dienstplan_addon Memory).
# Fallback-Zeitzonendatenbank fuer das stdlib-Modul "zoneinfo" (Schichtzeiten muessen
# korrekt als Europe/Berlin interpretiert werden) - falls das schlanke python:slim-Image
# kein System-tzdata mitbringt, greift Python automatisch auf dieses Paket zurueck.
tzdata==2024.2

# Am 29.07.2026 noetig geworden: das Vivendi-Login schickt ein "PublicKey"-Feld, dessen
# Erzeugung sich aus dem minifizierten Angular-Bundle nicht sicher rekonstruieren liess.
# Playwright bedient stattdessen das echte Login-Formular im echten (headless) Browser -
# der Mechanismus muss dadurch gar nicht verstanden werden. Deutlich schwereres Image
# (Chromium-Binary, siehe Dockerfile), aber robuster als ein Rateversuch bei der Auth.
playwright==1.47.0
